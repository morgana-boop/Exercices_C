#include <stdio.h>
    int main(){
        int x, a, b;
            printf("Digite o valor do numero; ");
                scanf("%d", &x);
                a = x - 1;
                b = x + 2;
            printf("Os valores são %d e %d", a, b);
    return 0;
    }