#include <stdio.h>
    int main(){
        int n, b;
            printf("Digite o valor inteiro: ");
                scanf("%d", &n);
            if (n > 0)
            {
                printf("O resultado é: %d\n", n);
            }
            else
                b = n * -1;
                printf("O resutado é: %d\n", b);
                
    return 0;
    }